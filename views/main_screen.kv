#:kivy 2.0
<MainScreen>:
    BoxLayout:
        orientation: 'vertical'
        padding: dp(10)
        spacing: dp(10)

        # Graph Layout, reserve space for graphs only
        BoxLayout:
            id: graph_layout
            orientation: 'vertical'
            size_hint_y: 0.6  # 60% for graphs

        # Buttons will be added dynamically in Python, so no buttons here

